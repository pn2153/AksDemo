apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rating-api.fullname" . }}-test-connection"
  labels:
    {{- include "rating-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rating-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
